#!/usr/bin/env tsx

/**
 * Schema Code Generator - Entry point.
 * Run with: pnpm run generate:schema
 * Watch mode: pnpm run generate:schema:watch
 */

import { main } from "./schema-generator";

main();
