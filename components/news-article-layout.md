# `news-article-layout.tsx`

Shared chrome for individual news article pages.

## Intent

- Provide consistent header area (back link, category badge, date, title) and
  footer (source links) across all article routes.
- Article content is passed as React children so each route owns its own copy.
