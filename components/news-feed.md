# `news-feed.tsx`

Card-based news listing for the `/nyheter` index page.

## Intent

- Show all articles newest-first as clickable cards.
- Each card links to the article's own route.
- Reads only lightweight metadata from `lib/news-articles.ts`.
