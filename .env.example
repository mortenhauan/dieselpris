# Optional: copy to `.env.local`. No variables required for local `pnpm dev`.
#
# CRON_SECRET — random string (16+ chars). Vercel sends it as Authorization: Bearer … when
# invoking `/api/revalidate-diesel`. Set on the Vercel project for production cron warming.
# CRON_SECRET=
